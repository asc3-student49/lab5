# Migrations

Numbered, append-only migration scripts. Do not edit a migration once it is committed — always add a new numbered migration instead.

Filename format: `NNNN_short_description.py` where NNNN is zero-padded.
